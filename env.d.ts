// No environment variable declarations in client-side code
// All sensitive environment variables are only accessed in server components/actions

// Define window.gapi for TypeScript if needed
interface Window {
  gapi: any
}
